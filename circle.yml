machine:
  environment:
    ANDROID_NDK_HOME: /home/ubuntu/android/android-ndk

dependencies:
  override:
    - echo $(pwd)

  cache_directories:
    - android-ndk-r10d

test:
  override:
    - ./gradlew assembleDebug --stacktrace