machine:
  environment:
    ANDROID_NDK_HOME: /home/ubuntu/raytracer/android-ndk-r10d

dependencies:
  override:
    - ./install_ndk.sh
  cache_directories:
    - android-ndk-r10d

test:
  override:
    - ./gradlew assembleDebug --stacktrace
