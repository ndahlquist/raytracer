machine:
  environment:
    ANDROID_NDK_HOME: /home/ubuntu/raytracer/android-ndk-r10d

dependencies:
  override:
    - ./ndk_installer.bin
  cache_directories:
    - android-ndk-r10d

test:
  override:
    - ./gradlew assembleDebug --stacktrace